# 高级视频质量评估依赖（可选）
# 这些包都有预编译版本，无需编译

# 感知相似度评估（最推荐）
lpips>=0.1.4

# 综合图像质量评估工具箱
pyiqa>=0.1.7

# 如果需要CLIP功能，使用官方库（避免编译问题）
# 安装方法: pip install git+https://github.com/openai/CLIP.git
# 注意: 不要安装 clip-score，它依赖需要编译的包

